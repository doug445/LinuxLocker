# Deployment artifacts — never commit these, they contain machine identity
luks-deploy-*.log
pre-luks-state-*/
LUKS-RECOVERY-*/
*.img
*.header
post-encryption.conf
*.bak
*.tmp

# Local SEO / launch scratch — not published
github-about.txt

# Agent scratch specs — the design docs that matter live in docs/design/
docs/superpowers/
